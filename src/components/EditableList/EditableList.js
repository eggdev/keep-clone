import React from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(theme => ({

}));

const EditableList = () => {
  const classes = useStyles();
	return (
    <>
		  <h1>EditableList Component</h1>
    </>
	);
};

export default EditableList;
